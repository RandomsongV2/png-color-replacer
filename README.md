# png color replacer
python file which changes selected colors in .png files of selected directory  
made for recoloring a lot of sprites split to different files (for example animation split to files walk-n.png)  
colors are configurable inside the file  
to use simply run the file from selected directory  
  
doesnt use any external modules, only built-in python
